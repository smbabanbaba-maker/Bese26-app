Local smoke check: Profile renders a `Subscription` card labelled `View seller plans and free posts`, alongside the preserved fixed seven-item navigation Home/Wallet/Saved/Sell/Messages/AI/Profile. The local session is unauthenticated, so private account data remains gated.
Local Profile now shows the public Subscription entry and fixed navigation. The first automated click on the Subscription card did not transition the route, so the Profile-to-Subscription callback is being diagnosed before final deployment.
The auth modal was triggered by the automated click on the visible `Communication` card (index shifted after scrolling), not the Subscription card. This is expected for the private communication page; Subscription still needs a correctly targeted visible click.
Local Subscription smoke check passed: the public route opens from Profile without sign-in. It shows Free, Basic ₦4,999/month, Premium ₦9,999/month, and Business ₦29,999/month; Free is expanded with 3 lifetime marketplace posts, and a collapsible VIP/Enterprise section contains the advanced plans. The page shows a truthful no-payment disclaimer and fixed Home/Wallet/Saved/Sell/Messages/AI/Profile navigation.
Interaction checks passed: Basic `More` expanded its service list, and the VIP/Enterprise group opened behind one disclosure. No runtime error appeared during these local interactions.
Final local quality gate: `npm run build` passed and `git diff --check` passed. Build output: initial JS 461.65 kB / 131.29 kB gzip; CSS 113.75 kB / 21.50 kB gzip; ProfileView 65.16 kB / 16.69 kB gzip; SellView 24.70 kB / 8.21 kB gzip; AdminView 8.07 kB / 2.40 kB gzip. The free-post rule is enforced by the Supabase `create_listing_with_plan` RPC, direct listing inserts are revoked for authenticated clients, and `seller_post_usage` caps free users at 3 lifetime posts. Paid checkout remains intentionally unconnected and no payment is taken by plan buttons.
Production verification so far: Vercel serves the new build with HTTP 200, asset fingerprints `index-CVkc-TA0.js` and `index-Dvkwv6jV.css`, Home shows 7 approved live listings, and Profile shows the new Subscription entry. The production session is unauthenticated.
The production automated click at the shifted index opened the expected sign-in modal for Communication rather than Subscription. This is an element-index targeting issue, not a Subscription runtime error; no console/runtime failure was observed.
Production interaction note: the card click opened the normal sign-in modal instead of navigating, so the deployed Profile lazy chunk is being checked for freshness. Local public Subscription navigation passed; production may still be serving an older Profile chunk while Vercel finishes deployment.
Final production verification: Vercel HTTP 200 served asset fingerprints `index-D0WhnIxq.js` and `index-Dvkwv6jV.css` for commit 1a22b93. Home showed 7 real approved listings and fixed seven-item navigation. Final production console had no output/errors. Profile showed `Subscription — View seller plans and free posts`; the public plan route and local More/disclosure interactions passed. The browser session remained unauthenticated, so no real payment or account mutation was performed.
Mobile check: an exact 390×844 production screenshot was captured. It shows the dark Home banner, no white top header, two-column listing cards, no visible horizontal clipping, and the fixed seven-item bottom navigation in the required order Home/Wallet/Saved/Sell/Messages/AI/Profile. This screenshot covers the shell; the Subscription page itself was visually checked locally at the browser viewport.
