{
  "name": "bese26-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "check": "npm test && npm run check:migrations && npm run check:secrets && npm run check:assets && vite build && npm run check:bundle",
    "check:assets": "node scripts/check_assets.mjs",
    "check:bundle": "node scripts/check_bundle_size.mjs",
    "check:migrations": "node scripts/check_migration_versions.mjs",
    "check:secrets": "node scripts/check_for_secrets.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.4",
    "@vitejs/plugin-react": "6.1.0",
    "lucide-react": "1.34.0",
    "react": "19.2.8",
    "react-dom": "19.2.8",
    "vite": "8.2.2"
  }
}
